<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Verservidores" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- Colores principales -->
        <item name="colorPrimary">@color/brand_primary1</item>
        <item name="colorOnPrimary">@color/on_primary1</item>

        <!-- Color de texto principal -->
        <item name="android:textColor">@color/text_main1</item>

        <!-- Color de fondo de ventana -->
        <item name="android:windowBackground">@color/background_main1</item>
    </style>

    <style name="Theme.Verservidores" parent="Base.Theme.Verservidores" />
</resources>